import Ember from 'ember';

export default Ember.Controller.extend({
  // var sclist = [];
  // for (i=0; i< model.harvestlog.length; i++){
  //   sclist.push(hl.sourceConfig.label);
  // 	console.log(hl.sourceConfig.label);
  // }
  // return sclist
});
