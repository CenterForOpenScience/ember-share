import Ember from 'ember';
// checked
export default Ember.Controller.extend({
  // queryParams: ['page'],
  // source_config_id: "2318C-922-51B",
  //
  // filteredHarvestLog: Ember.computed('source_config_id', 'model', function() {
  //  var source_config_id = this.get('source_config_id');
  //  var HarvestLog = this.get('model');
  //
  //  if (source_config_id) {
  //     return HarvestLog.filterBy('source_config_id', source_config_id);
  //   } else {
  //     return HarvestLog;
  //   }
  // })
 });
