{{outlet}}

<h2> Source Config Data</h2>

{{#each model.sourceconfig as |source-config|}}
	<li>1st loop</li>
	{{log source-config.name}}<br>

{{/each}}

{{#each sourceconfig as |source-config|}}
<li>2nd loop</li>
	{{source-config.source.name}}
{{/each}}
