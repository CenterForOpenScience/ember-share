{{outlet}}

<h1>STATUS BOARD</h1>
{{#each model.sourceconfig as |sc|}}
	<li>{{sc.id}}</li>
{{/each}}

<!-- use query method on the store
method on the model
route should be getting most of the
data fro that page
get the first page of harvest logs for each
source config -->
