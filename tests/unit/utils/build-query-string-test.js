import buildQueryString from 'ember-share/utils/build-query-string';
import { module, test } from 'qunit';

module('Unit | Utility | build query string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = buildQueryString();
  assert.ok(result);
});
